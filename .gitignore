# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.venv/
venv/
dist/
build/

# Node
node_modules/
frontend/dist/
.vite/
*.tsbuildinfo

# Env
.env
.env.local

# Editors / OS
.vscode/
.idea/
.DS_Store

# MANDATE local artifacts
*.mandate-save.json
/tmp/

# Playwright run state (Gate 4A3 harness). Transient: regenerated on every run and meaningless
# outside the machine that produced it, so it is never committed. The BASELINE ARTIFACTS are
# committed deliberately and live in docs/reviews/ instead.
frontend/test-results/
frontend/playwright-report/
