{
  "name": "mandate-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "MANDATE political strategy game \u2014 frontend. As of Phase 4A Gate 4A2 this is a playable vertical slice against the local game API (see docs/roadmap.md, Phase 4A).",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:bundle": "node tools/check-bundle.mjs",
    "_comment:postinstall": "tools/openapi-gen holds an isolated typescript@5 (see its package.json for why). node_modules/ is gitignored, so a clean checkout has no TS5 unless this runs -- and BOTH `generate:api:types` AND src/format/format-boundary.test.ts import from it. Without this, `npm ci && npm test` fails from a fresh clone and in CI.",
    "postinstall": "cd tools/openapi-gen && npm ci",
    "generate:api:schema": "cd ../backend && uv run python scripts/dump_openapi.py > ../docs/contracts/phase4a-openapi.json",
    "generate:api:types": "cd tools/openapi-gen && npx openapi-typescript ../../../docs/contracts/phase4a-openapi.json -o ../../src/api/schema.d.ts",
    "generate:api": "npm run generate:api:schema && npm run generate:api:types"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.101.4",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^7.0.0",
    "@testing-library/react": "^16.3.2",
    "@types/react": "^19.2.8",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.0.5",
    "jsdom": "^27.0.0",
    "tailwindcss": "^4.3.3",
    "typescript": "^7.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  }
}
