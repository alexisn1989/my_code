# Gate 4A3 accessibility baseline — axe-core in the real browser

**Generated by `npm run audit:accessibility`. Do not hand-edit.** This file is a rendering of
`gate-4a3-accessibility.json`, which the sweep writes as it runs.

**This is Commit 2 of six, and it RECORDS ONLY.** Nothing here is fixed. Per the frozen plan §9.1 the
sweep exits 0 whenever it completes, whatever it found: a non-zero exit would mean the harness broke,
never that the application has a defect. Triage and fixes are Commit 3, and each fix there cites a
finding id from this file. **No zero-violation regression test exists yet**, deliberately — adding one
now would commit a deliberately red suite.

## The headline

| | count |
|---|---|
| **WCAG 2.2 AA conformance findings (browser, screens)** | **3** |
| Beyond AA (AAA rules) | 0 |
| axe best-practice findings | 12 |
| Needs review (axe `incomplete`) | 6 |
| **Component-supplement findings** (`S1..Sn`) | **1** |
| Surfaces audited | 89 |
| Surfaces the navigation did not offer | 0 |
| Surfaces where axe itself failed (harness) | 0 |

## How this was measured

- **axe-core 4.13.0** via `@axe-core/playwright` 4.13.0, both pinned exactly in
  `package.json` the way `@playwright/test` is. The version is read off the installed package by
  the sweep itself, so this artifact records what actually ran rather than what someone believed was
  installed.
- **The real application, the real browser.** A production `mandate-gui` process serving the built
  SPA and the real scenarios, driven by Chromium 141.0.7390.37 (revision 1194) from the on-disk
  cache. `e2e/preflight.spec.ts` is a hard dependency of this project, so a run whose browser
  provenance cannot be proven does not produce findings at all.
- **Why not jsdom.** axe's most valuable rules need layout and resolved styles — `color-contrast`
  above all — and jsdom has neither, so it cannot evaluate them. That is why the frozen plan put
  Playwright before the accessibility work. The jsdom axe tests added alongside this commit are a
  labelled supplement for per-component regressions, **never** the primary evidence.
- **Every rule was enabled**, and each result is classified afterwards from its own tags: WCAG 2.2 AA
  tags make a **conformance** finding, AAA tags make a **beyond-AA** finding, and everything else is
  **best-practice**. Reporting axe's best-practice rules as conformance failures would inflate the
  baseline against a standard nobody committed to; hiding them would lose real signal. They are
  counted separately and both are listed in full.
- **`incomplete` is kept apart from `violations`.** An incomplete result is a question axe could
  not settle — usually an overlapped or non-text element it declined to judge — not a confirmed
  defect. Folding the two together would overstate the baseline by 6.

### The anti-vacuity guard, and why it is here

An axe run that fails to inject returns an **empty violations array**, which is indistinguishable
from a clean screen unless something else proves the rules ran. Commit 1a exists because exactly that
confusion produced a clean stress result for three screens that had never been stressed. So every
surface records `rulesEvaluated` — violations + passes + incomplete + inapplicable — and the run
**asserts it is non-zero for every audited surface**.

**Measured: all 89 surfaces evaluated between 90 and 93 rules.**
No result in this file rests on a silent no-op.

## Findings by rule

| rule | findings | impact | class |
|---|---|---|---|
| `region` | 12 | moderate | best-practice |
| `color-contrast` | 2 | serious | wcag-aa |
| `aria-valid-attr-value` | 1 | critical | wcag-aa |

**One finding is one `(screen, rule, node-set)` defect**, carrying every viewport case where it was
observed — not one finding per screen × viewport × rule. Most axe rules are viewport-independent: a
missing label or a contrast failure is the same defect at 1920 px and at 320 px, and numbering them
per viewport would multiply each by the 8 viewport cases and produce a count that
describes the sweep rather than the application. Nothing is lost, because a viewport-specific defect
is visible precisely by having a short `viewports` list.

## WCAG 2.2 AA conformance findings — the ones that matter for the gate

| id | screen | rule | impact | nodes | viewports | selector (first) |
|---|---|---|---|---|---|---|
| A8 | Decisions | `aria-valid-attr-value` | critical | 1 | 8/8 | `#policy-tab-Budget\ policy-taxation` |
| A1 | Dashboard | `color-contrast` | serious | 1 | 8/8 | `.h-48` |
| A9 | Decisions | `color-contrast` | serious | 11 | 8/8 | `div[aria-label="Policy choice"] > .aria-selected\:border-gold-500.aria` |


#### A8 — `aria-valid-attr-value` on Decisions (critical)

- **Rule:** ARIA attributes must conform to valid values
- **Reference:** https://dequeuniversity.com/rules/axe/4.13/aria-valid-attr-value?application=playwright
- **WCAG tags:** wcag2a, wcag412
- **Failing nodes:** 1
- **Observed at:** desktop-1920x1080, laptop-1440x900, narrow-820x900, mobile-390x844, reflow-wcag-320x512, reflow200-desktop-960x540, reflow200-laptop-720x450, reflow200-mobile-195x422
- **Selectors:**
  - `#policy-tab-Budget\ policy-taxation`
- **axe says:** Fix all of the following: Invalid ARIA attribute value: aria-controls="policy-panel-Budget policy-taxation"

#### A1 — `color-contrast` on Dashboard (serious)

- **Rule:** Elements must meet minimum color contrast ratio thresholds
- **Reference:** https://dequeuniversity.com/rules/axe/4.13/color-contrast?application=playwright
- **WCAG tags:** wcag2aa, wcag143
- **Failing nodes:** 1
- **Observed at:** desktop-1920x1080, laptop-1440x900, narrow-820x900, mobile-390x844, reflow-wcag-320x512, reflow200-desktop-960x540, reflow200-laptop-720x450, reflow200-mobile-195x422
- **Selectors:**
  - `.h-48`
- **axe says:** Fix any of the following: Element has insufficient color contrast of 3.09 (foreground color: #63615c, background color: #0a0f1a, font size: 12.0pt (16px), font weight: normal). Expected contrast ratio of 4.5:1

#### A9 — `color-contrast` on Decisions (serious)

- **Rule:** Elements must meet minimum color contrast ratio thresholds
- **Reference:** https://dequeuniversity.com/rules/axe/4.13/color-contrast?application=playwright
- **WCAG tags:** wcag2aa, wcag143
- **Failing nodes:** 11
- **Observed at:** desktop-1920x1080, laptop-1440x900, narrow-820x900, mobile-390x844, reflow-wcag-320x512, reflow200-desktop-960x540, reflow200-laptop-720x450, reflow200-mobile-195x422
- **Selectors:**
  - `div[aria-label="Policy choice"] > .aria-selected\:border-gold-500.aria-selected\:text-gold-500[role="tab"]:nth-child(1) > .text-parchment-200\/50`
  - `div[aria-label="Policy choice"] > .aria-selected\:border-gold-500.aria-selected\:text-gold-500[role="tab"]:nth-child(2) > .text-parchment-200\/50`
  - `.aria-selected\:border-gold-500.aria-selected\:text-gold-500[role="tab"]:nth-child(3) > .text-parchment-200\/50`
  - `#policy-tab-Budget\ policy-taxation > .text-parchment-200\/50`
  - `#policy-tab-Budget\ policy-spending > .text-parchment-200\/50`
  - `div[aria-label="Raise the personal income tax"] > .text-\[10px\].uppercase.tracking-wide`
- **axe says:** Fix any of the following: Element has insufficient color contrast of 4.16 (foreground color: #7c7973, background color: #0f1626, font size: 10.5pt (14px), font weight: normal). Expected contrast ratio of 4.5:1



## Best-practice findings

All 12 share a single rule (`region`) and a single pair of selectors, so they are **one shell-level defect observed on every screen**, not 12 independent problems. Commit 3 should expect one fix to clear the whole set.

| id | screen | rule | impact | nodes | viewports | selector (first) |
|---|---|---|---|---|---|---|
| A10 | Decisions | `region` | moderate | 2 | 8/8 | `.items-start > div` |
| A11 | Turn result | `region` | moderate | 2 | 8/8 | `.items-start > div` |
| A12 | History | `region` | moderate | 2 | 8/8 | `.items-start > div` |
| A13 | Strategic map | `region` | moderate | 2 | 8/8 | `.items-start > div` |
| A14 | Victory / defeat | `region` | moderate | 2 | 8/8 | `.items-start > div` |
| A15 | Glossary | `region` | moderate | 2 | 1/8 | `.items-start > div` |
| A2 | Dashboard | `region` | moderate | 2 | 8/8 | `.items-start > div` |
| A3 | Government | `region` | moderate | 2 | 8/8 | `.items-start > div` |
| A4 | Economy | `region` | moderate | 2 | 8/8 | `.items-start > div` |
| A5 | Legislature | `region` | moderate | 2 | 8/8 | `.items-start > div` |
| A6 | Constitution | `region` | moderate | 2 | 8/8 | `.items-start > div` |
| A7 | Relationships | `region` | moderate | 2 | 8/8 | `.items-start > div` |



## Needs review — axe could not decide

These are **not** counted as violations. Each is a node axe declined to judge, usually because it is
overlapped, partially obscured, or contains only non-text characters. Commit 3 should resolve each by
inspection rather than by assuming either outcome.

| id | screen | rule | impact | nodes | viewports | selector (first) |
|---|---|---|---|---|---|---|
| N1 | Dashboard | `color-contrast` | serious | 5 | 8/8 | `section:nth-child(1) > .text-xl.tabular-nums > .text-parchment-200 > s` |
| N2 | Decisions | `color-contrast` | serious | 6 | 7/8 | `div[aria-label="Raise the personal income tax"] > .pt-2.border-t.gap-1` |
| N3 | Strategic map | `color-contrast` | serious | 6 | 3/8 | `text[data-theater-label="marnil_border"]` |
| N4 | Relationships | `color-contrast` | serious | 36 | 1/8 | `button[data-leader-option="leader_opposition_party"] > .gap-0\.5.flex-` |
| N5 | Decisions | `color-contrast` | serious | 15 | 1/8 | `div[aria-label="Raise the personal income tax"] > .pt-2.border-t.gap-1` |
| N6 | Victory / defeat | `color-contrast` | serious | 1 | 1/8 | `.gap-3.flex > .py-1:nth-child(2)` |


#### N1 — `color-contrast` on Dashboard (serious)

- **Rule:** Elements must meet minimum color contrast ratio thresholds
- **Reference:** https://dequeuniversity.com/rules/axe/4.13/color-contrast?application=playwright
- **WCAG tags:** wcag2aa, wcag143
- **Failing nodes:** 5
- **Observed at:** desktop-1920x1080, laptop-1440x900, narrow-820x900, mobile-390x844, reflow-wcag-320x512, reflow200-desktop-960x540, reflow200-laptop-720x450, reflow200-mobile-195x422
- **Selectors:**
  - `section:nth-child(1) > .text-xl.tabular-nums > .text-parchment-200 > span[aria-hidden="true"]`
  - `section:nth-child(2) > .text-xl.tabular-nums > .text-parchment-200 > span[aria-hidden="true"]`
  - `section:nth-child(3) > .text-xl.tabular-nums > .text-parchment-200 > span[aria-hidden="true"]`
  - `section:nth-child(4) > .text-xl.tabular-nums > .text-parchment-200 > span[aria-hidden="true"]`
  - `section:nth-child(5) > .text-xl.tabular-nums > .text-parchment-200 > span[aria-hidden="true"]`
- **axe says:** Fix any of the following: Element content contains only non-text characters

#### N2 — `color-contrast` on Decisions (serious)

- **Rule:** Elements must meet minimum color contrast ratio thresholds
- **Reference:** https://dequeuniversity.com/rules/axe/4.13/color-contrast?application=playwright
- **WCAG tags:** wcag2aa, wcag143
- **Failing nodes:** 6
- **Observed at:** desktop-1920x1080, laptop-1440x900, narrow-820x900, mobile-390x844, reflow-wcag-320x512, reflow200-desktop-960x540, reflow200-laptop-720x450
- **Selectors:**
  - `div[aria-label="Raise the personal income tax"] > .pt-2.border-t.gap-1 > .text-parchment-200\/80.justify-between.items-center > .tabular-nums.gap-1.items-center > span[aria-hidden="true"]`
  - `div[aria-label="Lower the personal income tax"] > .pt-2.border-t.gap-1 > .text-parchment-200\/80.justify-between.items-center > .tabular-nums.gap-1.items-center > span[aria-hidden="true"]`
  - `div[aria-label="Raise the corporate tax"] > .pt-2.border-t.gap-1 > .text-parchment-200\/80.justify-between.items-center > .tabular-nums.gap-1.items-center > span[aria-hidden="true"]`
  - `div[aria-label="Lower the corporate tax"] > .pt-2.border-t.gap-1 > .text-parchment-200\/80.justify-between.items-center > .tabular-nums.gap-1.items-center > span[aria-hidden="true"]`
  - `div[aria-label="Raise the consumption tax"] > .pt-2.border-t.gap-1 > .text-parchment-200\/80.justify-between.items-center > .tabular-nums.gap-1.items-center > span[aria-hidden="true"]`
  - `div[aria-label="Lower the consumption tax"] > .pt-2.border-t.gap-1 > .text-parchment-200\/80.justify-between.items-center > .tabular-nums.gap-1.items-center > span[aria-hidden="true"]`
- **axe says:** Fix any of the following: Element content contains only non-text characters

#### N3 — `color-contrast` on Strategic map (serious)

- **Rule:** Elements must meet minimum color contrast ratio thresholds
- **Reference:** https://dequeuniversity.com/rules/axe/4.13/color-contrast?application=playwright
- **WCAG tags:** wcag2aa, wcag143
- **Failing nodes:** 6
- **Observed at:** desktop-1920x1080, laptop-1440x900, reflow200-desktop-960x540
- **Selectors:**
  - `text[data-theater-label="marnil_border"]`
  - `text[data-theater-label="sorrend_plain"]`
  - `text[data-theater-label="valdrun_capital"]`
  - `text[data-theater-label="valdrun_east"]`
  - `text[data-theater-label="valdrun_highlands"]`
  - `text[y="-330"]`
- **axe says:** Fix any of the following: Element's background color could not be determined because it is overlapped by another element

#### N4 — `color-contrast` on Relationships (serious)

- **Rule:** Elements must meet minimum color contrast ratio thresholds
- **Reference:** https://dequeuniversity.com/rules/axe/4.13/color-contrast?application=playwright
- **WCAG tags:** wcag2aa, wcag143
- **Failing nodes:** 36
- **Observed at:** reflow200-mobile-195x422
- **Selectors:**
  - `button[data-leader-option="leader_opposition_party"] > .gap-0\.5.flex-col.flex > .text-parchment-100`
  - `span[data-leader-question="leader_opposition_party"]`
  - `span[data-leader-refusal="leader_opposition_party"]`
  - `button[data-leader-option="leader_opposition_party"] > .gap-0\.5.flex-col.flex > .text-parchment-200\/60.text-xs`
  - `button[data-counterpart-option="marnil"] > .gap-0\.5.flex-col.flex > .text-parchment-100`
  - `span[data-counterpart-question="marnil"]`
- **axe says:** Fix any of the following: Element's background color could not be determined because it partially overlaps other elements

#### N5 — `color-contrast` on Decisions (serious)

- **Rule:** Elements must meet minimum color contrast ratio thresholds
- **Reference:** https://dequeuniversity.com/rules/axe/4.13/color-contrast?application=playwright
- **WCAG tags:** wcag2aa, wcag143
- **Failing nodes:** 15
- **Observed at:** reflow200-mobile-195x422
- **Selectors:**
  - `div[aria-label="Raise the personal income tax"] > .pt-2.border-t.gap-1 > .text-parchment-200\/80.justify-between.items-center > .tabular-nums.gap-1.items-center`
  - `div[aria-label="Raise the personal income tax"] > .pt-2.border-t.gap-1 > .text-parchment-200\/80.justify-between.items-center > .tabular-nums.gap-1.items-center > span[aria-hidden="true"]`
  - `div[aria-label="Lower the personal income tax"] > .pt-2.border-t.gap-1 > .text-parchment-200\/80.justify-between.items-center > .tabular-nums.gap-1.items-center`
  - `div[aria-label="Lower the personal income tax"] > .pt-2.border-t.gap-1 > .text-parchment-200\/80.justify-between.items-center > .tabular-nums.gap-1.items-center > span[aria-hidden="true"]`
  - `div[aria-label="Raise the corporate tax"] > .pt-2.border-t.gap-1 > .text-parchment-200\/80.justify-between.items-center > .tabular-nums.gap-1.items-center`
  - `div[aria-label="Raise the corporate tax"] > .pt-2.border-t.gap-1 > .text-parchment-200\/80.justify-between.items-center > .tabular-nums.gap-1.items-center > span[aria-hidden="true"]`
- **axe says:** Fix any of the following: Element's background color could not be determined because it partially overlaps other elements

#### N6 — `color-contrast` on Victory / defeat (serious)

- **Rule:** Elements must meet minimum color contrast ratio thresholds
- **Reference:** https://dequeuniversity.com/rules/axe/4.13/color-contrast?application=playwright
- **WCAG tags:** wcag2aa, wcag143
- **Failing nodes:** 1
- **Observed at:** reflow200-mobile-195x422
- **Selectors:**
  - `.gap-3.flex > .py-1:nth-child(2)`
- **axe says:** Fix any of the following: Element's background color could not be determined because it's partially obscured by another element



## Component-supplement findings (`S1..Sn`) — what a screen sweep structurally could not find

The jsdom supplement (`src/greybox/a11y.components.test.tsx`) audits shared primitives in
isolation. It is **not** the primary evidence and cannot be: jsdom has no layout engine, so it cannot
evaluate `color-contrast` or anything else needing a rendered box. But it can reach a component the
screens do not, and that is exactly what it did.

### S1 — `aria-required-attr` in RatioBar (critical, wcag-aa)

- **File:** `src/greybox/components.tsx`
- **Rule:** Required ARIA attributes must be provided
- **Reference:** https://dequeuniversity.com/rules/axe/4.13/aria-required-attr
- **Defect:** role="meter" is declared with aria-label and aria-valuetext but WITHOUT the required aria-valuenow, so assistive technology is given a meter with no current value.
- **Why the browser sweep missed it:** Nothing in src/ renders RatioBar. It is exported from components.tsx and has no call site, so no screen can exhibit the defect and no screen-level audit could ever have found it. A test asserts that call-site absence, so if a screen starts using it this finding becomes user-facing and must appear in the browser baseline too.
- **Commit 3 options:** Add aria-valuenow (the projected ratio is already available), or delete the unused component. Either way the characterisation test in a11y.components.test.tsx must be updated, which is the intended pressure.


These are declared in `src/greybox/a11y.supplement-findings.json`, which the supplement test imports
and asserts against axe's actual output — so this section cannot claim a defect that does not
reproduce, and the test cannot quietly stop covering one that does.

## What was measured, and what was found sound

- 89 surface(s) audited, each evaluating between 90 and 93 axe rules — so every recorded result rests on rules that demonstrably ran
- no surface was violation-free: every audited surface carries at least one finding below, which is a statement about the application and not a gap in the sweep

## Surfaces not audited, and why — the two reasons kept apart

A screen the **navigation** did not offer is a fact about the application and relaxes the coverage
threshold. A surface where **axe itself failed** is the harness breaking, and the sweep asserts that
list is empty — while the two shared one list, an axe crash silently lowered the expected coverage and
the run still passed, which is the accommodate-the-gap failure Commit 1a exists to correct.

**Navigation did not offer:** _none — every screen was reached at every viewport case._

**axe failed to run:** _none — axe evaluated rules successfully on every surface it reached._


## Coverage

- Screens audited: Dashboard, Government, Economy, Legislature, Constitution, Relationships,
  Decisions, Turn result, History, Strategic map, Victory / defeat, plus the chrome-level Glossary.
  The three that still render `UnavailableScreen` are audited too — an unavailable screen is still a
  surface a keyboard and a screen reader reach.
- Viewport cases: desktop-1920x1080, laptop-1440x900, narrow-820x900, mobile-390x844, reflow-wcag-320x512, reflow200-desktop-960x540, reflow200-laptop-720x450, reflow200-mobile-195x422.
- `reflow200-mobile-195x422` is **below** the 320 px floor WCAG 2.2 SC 1.4.10 sets, so it is a real
  user observation and not a conformance target — the distinction Commit 1 drew and this sweep
  inherits.
- Totals: **15 finding(s)** (3 conformance,
  0 beyond-AA, 12 best-practice), **6 needing
  review**, across **89 audited surface(s)**.
